# Session 2026-08-25/26 — RU.TV v2.10 → v2.11: финальные волны Марго и Стаса

**Ветка:** `feat/rutv-showcase-v2` · **Коммиты:** `6ef5653` (v2.10) + `967f5bc` (v2.10.1 Likee) + v2.11 с этим sync · **Работа через worktree** (основное дерево на `feat/gorod-home-rmg-storefront`, не тронуто).

## Вход

`resume rutv` + «глянь в чате DFM финальные правки, дай standalone в zip в Saved». Контекст дня: Стас в ЛС — «давай РУТВ по бырому доделаем», «ща Рита пришлет. И надо будет в фигму перенести уже для Лёши после того как он глянет творение».

## v2.10 — 8 пунктов Марго (DFM #617118, 25.08 11:04, реплай на v2.9)

1. **Чарт «Супер 20» вертикально**: grid-auto-flow column — главная 2×5 (1-5 слева, 6-10 справа), полная 2×10; разделитель над верхом правого столбца погашен `:nth-child(6/11)`; ≤1100px возврат в row-flow (иначе column-flow с одной колонкой раскидал бы в implicit-колонки).
2. **Соцсети +Like +RuTube**: круги 52→46px, гэп 12→10 — пять штук в колонку 300px (панель scrollWidth==clientWidth, помещается впритык); RUTUBE малым кеглем `.soc-btn-sm` 8.5px.
3. **Кнопки под трансляцией убраны**: «Смотреть эфир» и бонус-сундук — сундук-механика выпилена ЦЕЛИКОМ (CSS+JS+правило «+50» на витрине; CHEST_TOTAL/chestReady/тосты); +10-тик и Витрина живы; эфир = клик по экрану/PiP.
4. **Счётчик просмотров** с трансляции снят (маркап+CSS).
5. **«Программы» = двухколонник**: `.programs-layout` grid `minmax(0,1fr) 360px` — сетка программ слева (поднята), Телепрограмма блоком справа; ≤1100px одна колонка, программы первыми.
6. **Программы = реальная линейка ru.tv/programms** (ссылку дала сама Марго): страница — SPA, данные вытащены из `window.__NUXT__`-пейлоада (unicode_escape-декод; grep кириллицы в дампе — только через utf-8-файл). 9 программ: Супер 20 · Тема · Статус: в сети · RUновости · Дискотека Дискотек · Топ-чарт Rocket Records · Сияй по-звёздному · Личный вопрос · Дотянись до звёзд; описания сжаты из текстов сайта, ведущие настоящие (Курдюкова/Юрьев/Голден/Мустафаева). Карточки 4:3 («чуть горизонтальные»), desc line-clamp 3. Телепрограмма пересобрана из линейки; реальные слоты с сайта: «Статус: в сети» будни 13:00-15:00, «Дискотека Дискотек» вс 22:00 — остальное демо (RUTV-137).
7. **«Ностальгия» → «Подборки»** (RUTV-139 закрыт — имя выбрала Марго; route-id nostalgia внутренний; жанровый тег карточки 2000-х «Ностальгия» → «Хиты нулевых», чтобы слово не путалось с вкладкой).
8. **Стили = Glass + белый**: Apple Dark удалён regex-пассом (19 CSS-блоков) + обе кнопки; гард `applyStyle`: всё не-light → glass (localStorage шарится между file://-страницами — сохранённый «apple» гасится).

**Следствие-аудит**: демо-события/видео со снятыми выдуманными программами переименованы (Звёзды Хайпа → Дискотека Дискотек и т.п.); **фулл-скрин поймал: креатив со вшитым текстом «Звёзды Хайпа» (Rectangle 4322-2.png)** — заменён нейтральными 4322-3 (огни) / 4322-9 / 4322-10. Урок = класс ловушки SHAMAN-кадра: **после правки ДАННЫХ смотреть КАРТИНКИ, в которые они завёрнуты**.

## v2.10.1 — бренд-сверка Likee (26.08, вопрос owner'а «уверен что верные лого?»)

Лого нигде и не было — типографика (канон RUTV-138). Но состав проверен по первоисточникам: «Like» Марго = **Likee** (RU.TV ведёт аккаунт — их новость «Телеканал РУ.ТВ запустил большой челлендж в Likee»; бренд пишется с двумя «e») → глиф и футер Like→Likee. RuTube подтверждён: официальный канал rutube.ru/u/rutv (851 видео), вордмарк RUTUBE.

## v2.11 — две правки Стаса после форварда (DFM 617668-676, 26.08 утро)

- **617668+617672**: «задний фон в стеклянной теме на что-то нейтральное… может градиенты какие-то» → glass `.app-backdrop` = нейтральный градиент: 3 мягких радиальных свечения бренд-палитры (#E40087 .30 / #7a2be2 .26 / #21B5FF .16) на тёмной вертикали #120b1a→#08070d; кадр Лазарева с фона снят (в САМОМ плеере остался — это эфир), затемняющий `::after` не нужен — удалён; мёртвый `dynamic-backdrop` (JS его никогда не включал) удалён.
- **617674 (фото пилюли ⭐ 2 260) + 617675 «скроем»**: пилюля баллов из топбара удалена (маркап+CSS+`tbPointsVal/Float` из renderPoints); механика жива — баланс на Витрине подарков (пункт меню), тикер «+10 через…» в PiP.

## Верификация (обе волны)

- DOM-аудит числом: колонки чарта по offsetLeft (1-5/6-10 и 1-10/11-20), соцсети = [VK,OK,MAX,Likee,RUTUBE] и overflow 0 у каждого круга, sched правее и уже (360px) сетки программ, аспект арта 1.33, кнопок/счётчика/пилюли нет, фон glass = 4 градиента и 0 url(), меню и тайтлы «Подборки».
- Гард-тест: localStorage style=apple + reload → glass.
- Глазами: Glass+Light × 1920 (главная fullpage, Программы, соц-панель крупно) + ~500px (окно Windows уже не жмётся).
- Консоль 0 (единственная строка v2.11 — известный артефакт `resize_page`/evaluate «Unsafe attempt to load URL .html», задокументирован ещё в v2.6).
- `node --check` объединённого скрипта = 0; standalone `grep assets/` = 0, смоук инлайновости data:URI.

## Доставка (все — telethon `send_file("me")` явно, без caption)

| Версия | Saved | Статус |
|---|---|---|
| v2.10 zip / HTML | 617203 / 617659 | устарели (там «Like») |
| v2.10.1 zip / HTML | 617664 / 617665 | устарели (фон-кадр, пилюля) |
| **v2.11 zip / HTML** | **617679 / 617680** | **АКТУАЛЬНОЕ — форвардить HTML** |

Эльбик форвардит сам (Марго смотрит именно HTML). Копии: `deliverables/2026-08-25/` основного дерева. Эльбик уже отправил v2.10.1 HTML в DFM (617666 «готово») — после v2.11 стоит переслать свежий файл.

## Состояние после сессии

- NEXT.md/DEBT.md ветки обновлены этим sync (DECISIONS 25.08 + блок v2.10–v2.11, RUTV-139 done, +RUTV-141 градиент-сверка, +RUTV-142 состав соцсетей).
- Shared memory: `project_design_rutv_showcase_v2.md` дополнен v2.10/v2.10.1/v2.11 + индекс.
- ⏭ СЛЕДУЮЩЕЕ: реакция DFM на v2.11 → **перенос в фигму для Лёши после его просмотра** (Стас ЛС 617114; его фигма: RMG-web-2.0, node 19-2, ссылка в ЛС 615864; плагин figma у Стаса — инструкция дана в ЛС 615866).
- Открыто: MXM-002 (иконка «Магазин» maximum.ru за Эльбиком) · RUTV-124/127/130/137/138/140/141 · заказчиковые 121/122/123(обновлён)/125/126/129/131/134/142 · twinr-сигнал Стаса про рекламный кабинет (613227) — не rutv.

## Рельсы/ловушки, подтверждённые сессией

- Чат: `.scratch/rutv3/read_chats.py` (дамп в utf-8-файл, cp1251-консоль ломает кириллицу и в выводе python — писать file.write, не print).
- ru.tv — Nuxt-SPA: контент в `window.__NUXT__` (unicode_escape), соц-ссылки в пейлоаде НЕТ (рантайм-API) — состав соцсетей проверять WebSearch'ем.
- `grep -c` с нулём совпадений роняет `&&`-цепочку (exit 1) — не ставить перед сборкой.
- Chrome держит хэндл на worktree-папку и после закрытия вкладки — `git worktree remove` со второй попытки/`rm -rf` + `git worktree prune`.

## v2.11.1 → ОТКЛОНЕНА; v2.12 — «как на DFM» (позже 26.08)

- v2.11.1 (умный угол PiP на «Программах») Стас отклонил: 617722 «да нее». Урок: **чинить симптом расположением оверлея — слабый ход; заказчик хотел другую СХЕМУ страницы.**
- Стас 617724-726: «давай с тобой еще финально сами посмотрим… **как на DFM, только в твоей стилистике** https://dfm.ru/schedule — **сверху расписание, снизу блоки с программами**».
- Референс декодирован скрином dfm.ru/schedule: матрица неделей — строки-дни («24.08 Пн»), плитки слотов «название + время + длительность», активный слот залит фирменным цветом.
- **v2.12**: телепрограмма-матрица сверху (7 строк Пн-Вс реальной текущей недели, даты dd.mm; плитки glass; «Сейчас» = акцент-заливка + пульс-точка, только в сегодняшней строке, ночной 00:00 в конце и вне сканера; длительность до следующего слота, за полночью +24ч, последний «до утра»; строки overflow-x на узких без скроллбара) + сетка программ снизу. Двухколонник v2.10-схемы и corner-хак v2.11.1 сняты, PiP снова br (конфликт исчез со сменой схемы). Начатый было док-в-колонку вариант откачен ДО коммита (git checkout) — Стас успел задать направление, пока строился док.
- Saved: **zip 617738 + HTML 617739 — АКТУАЛЬНОЕ** (617715/617716 и всё раньше устарели). Марго (617703) спрашивала «отправлять рутв?» — Стас финалит с Эльбиком напрямую (617724).
- Ловушка сессии: bash-heredoc с большими python-строками падает на кавычках — большие правки писать .py-файлом в scratchpad; упавший скрипт может успеть отработать ЧАСТЬ rep'ов только если запись в конце — при ассерте до записи файл не тронут (перепроверять грепом, что реально заменилось).

## v2.13 — «горизонтально по дням» (сразу после v2.12)

Стас 617728 (пришло через минуту после «окей», построить v2.12 успели раньше): фото-мокап — три копии НАШЕГО вертикального списка рядом + «Горизонтально по дням наверное лучше». Прочтение: дни = колонки слева направо, внутри каждой — прежний вертикальный список. → v2.13: `.smx-col` 368px в горизонтальной ленте (overflow-x, scroll-snap, тонкий скроллбар), в колонке — список в возвращённых стилях v2.10 (время | название+мета, «Сейчас» = accent-soft строка + live-badge), сегодняшняя колонка — inset-кольцо розовым + авто-scrollLeft в кадр; «Сейчас» только в сегодняшней; длительности v2.12 убраны (в мокапе только время), slotDur удалён. Verified: 7 колонок, даты 24.08-30.08, автоскролл (scrollLeft 770 @1920), «Сейчас»=Статус: в сети 13:00 (Ср 13:0x), Light ок, мобила 500px (лента скроллится, страница нет), консоль 0 (артефакт resize задокументирован). Saved: **zip 617740 + HTML 617741 — АКТУАЛЬНОЕ** (617738/617739 устарели, жили 20 минут).

## v2.13.1 — нота и переполнение (owner, поздний день)

Owner: «"Сетка — демо: программу передач пришлёт редакция" — лишнее» + «как будет вести себя каждый блок, если значений больше? скролл?» → (а) дисклеймер из видимой ноты убран (факт в RUTV-137); (б) потолок колонки-дня: .smx-list max-height 574px + overflow-y auto + overscroll-contain, тонкий scrollbar-color; сегодняшний список автоподкручен к «Сейчас» (rect-based delta). Перегруз-тест клонами до 20+ слотов: колонка 574px, скролл внутри, «Сейчас» в кадре, страница не тянется. (в) Пойман свой баг: при init renderSched считал scrollLeft/scrollTop на скрытой странице (display:none → нулевая геометрия) → авто-позиционирование в реальном флоу не срабатывало; фикс — setRoute('programs') перезапускает renderSched на видимой странице. ⚠️ Урок: geometry-зависимые эффекты рендера ВСЕГДА гонять честным флоу навигации, а не ручным вызовом после перехода (ручной вызов в тесте замаскировал баг v2.13). Saved: zip 617744 + HTML 617745 — АКТУАЛЬНОЕ (617740/617741 устарели).

## Фигма-файл: полный комплект + QA (26.08, вечер)

- «RU.TV — сайт (макеты)» https://www.figma.com/design/kn7zZnwAHCdWEzCS1KpIYv дорос до **41 фрейма**: 4 ряда (десктоп Glass 01-10 / десктоп Светлая L01-L10 / мобила 390 Glass M01-M10 / мобила Светлая ML01-ML10) + стейт «00 Меню · drawer». Мобильные — эмуляция chrome-devtools `viewport 390x844x2,mobile,touch`, капчер по той же localhost-рельсе.
- QA дробей (вопрос owner'а «589.77»): аудит 16 098 нод → 10 330 дробных; 3 пасса округления (сверху вниз; паддинги-источники; снизу вверх для HUG-родителей) → ~9.5к нод в целых; остаток 2 948 — производные (FILL-дети: родитель делит ширину как 1fr, 426.33 = треть контейнера; авто-тексты от шрифтовой метрики) — «чинить» их = ломать резину. Верификация: pixel-diff главной до/после 1.6% (антиалиасинг полупиксельных сдвигов), глазом идентично.
- Призраки (вопрос owner'а «скрытый Sidebar висит и мешает»): «Sidebar - Меню» (off-canvas drawer, opacity 0) сидел во всех 40 фреймах — удалён везде, один экземпляр вынесен видимым фреймом-стейтом (49:2; внутри снято 77 скрытий). Прочие off-canvas (~148 Article/Text) = карточки рейлов за вьюпортом, легитимный скролл-контент — не трогать.
- Доступ: Стас запросил view — owner одобрил сам через Figma UI (Chrome в фигму не залогинен; API правами не управляет — ограничение задокументировано).
- Рельсы-уроки: HUG-родителей округлять ПОСЛЕ детей (обход reverse); FILL/авто-текст детектить ДО «починки»; generate_figma_design не поддерживает file:// — только localhost; окно ресайзить/эмулировать ДО навигации на capture-URL.

## v2.14 — квадратные постеры программ + фигма-синк (26.08 веч., отдельная сессия `resume rutv`)

**Вход**: «аудит прошлой сессии как собрали фигма-файл + глянь коммент Стаса и учти в файле».

**Аудит фигма-файла (прошлой сессии)**: структура эталонная — 41 фрейм, 4 ряда (y=0/3373/6746/10651, шаг x=1961 десктоп / 430 мобила), все топ-позиции/размеры целые, стейт меню 49:2 на месте. **Комментов в самом фигма-файле НЕТ** (панель Comments пустая — проверено браузером; юзерский Chrome в фигму ЗАЛОГИНЕН, вопреки записи прошлой сессии «не залогинен» — та была про UI-одобрение доступа). ⚠️ В фигме баннер «Your invoice is past due» (план Simbio) — оплату проверить.

**«Коммент Стаса» = волна в чате DFM после расшаривания файла (617784-617812)**:
- 617784 Стас: «давай в фигме отрисуем экраны все и мобильную версию» — уже сделано прошлой сессией (41 фрейм).
- 617785-786 Лёша (@MeoQi): скрин наших карточек программ «здесь будет какой размер?» + скрин ru.tv «родные постеры там сейчас широкие» → 617787 Стас (фото РР): «Как на РР первый блок со станциями» → 617789 Лёша: «квадраты 1000*1000 ок» → 617791 Стас: «чем меньше ресайзов по всем проектам, тем проще всем». 617788 Стас: «я попрошу перерисовать» (ассеты за RU.TV).
- 617792-793: страницы ведущих пустые — Лёша заведёт, описания/фото unифицирует RU.TV.
- **617812 Лёша (после доступа к фигме): «Внутрянки нужны. Особенно программ. В контактах вкладка "Обратная связь". Плеер потока/материала — все элементы управления. Новость / персона. И навигацию посмотреть. Может крошки добавить… Тот же выход из новости назад»** → RUTV-144, следующий большой заход.

**Сделано (v2.14)**: `.card-program-art` aspect 4:3 → **1:1** (единственная правка кода; рейл главной 232×232, грид «Программ» 211×211 — проверено числом и глазом в обеих темах). **Фигма-синк**: 8 фреймов (01/L01/M01/ML01 + 02/L02/M02/ML02) перекапчерены и заменены на местах; страница → «RU.TV — макеты сайта (v2.14, 26.08)». Поставка: **zip → Saved 617821 + голый HTML → Saved 617822** (617744/617745 устарели). Копии в `deliverables/2026-08-26/` основного дерева.

**Рельса-апгрейд капчера**: `generate_figma_design` принимает `nodeId` (страница 0:1) — капчер ложится СРАЗУ на нашу страницу, без промежуточных страниц и переносов; captureId можно генерить пачкой и капчерить параллельно (файл уже существует). capture.html пересоздан (в git его нет — пересоздавать каждый раз: инъект capture.js + чтение ?route=/?style= в localStorage ДО инита SPA). Хэш-URL: `#figmacapture=<id>&figmaendpoint=<urlencoded submit>&figmadelay=2500`. Новые капчеры приносят СВОЙ призрак «Sidebar - Меню» (opacity 0) — удалять после каждого капчера; «Sidebar - Реклама» (главная) и «Sidebar - Мини-плеер» (программы, PiP) — легитимные видимые. Округление дробей: один bottom-up-пасс на фрейм (~220 нод десктоп / ~90-210 прочие; TEXT — ceil, GROUP/BO — только позиции, FILL/авто-тексты пропуск) даёт паритет с 3-пассовым QA прошлой сессии.

## v2.15 — внутрянки Лёши (26.08 ночь, «го все» owner'а)

RUTV-144 построен целиком: 7 экранов (программа-внутрянка · плеер эфира · плеер клипа · новость · артист · ведущий · Контакты+«Обратная связь») + крошки/«Назад»/делегат кликов + кнопка ⤢ на hero. Данные: news[].body (3 абзаца ×6), генератор выпусков programEpisodes (детерминированный), scheduleSlotsFor (слоты из телепрограммы), демо-связки ведущий↔программы. Verified: 12-шаговый флоу-тест честными кликами, обе темы, мобила (hScroll 0), консоль 0 ошибок, node --check 0. Ловушка поймана: teater-плеер сперва не влезал в вьюпорт (контролы за фолдом) → width по высоте вьюпорта; PiP перекрывал контролы плеера → на watch/live-view окошко скрыто (материал/эфир играют сами).

**Фигма: 36 капчеров одной сессией** (конвейер на 4 Chrome-страницах: 2 десктоп-эмуляции 1920 + 2 мобилы 390; ID пачками по 4; capture-инъект расширен ?detail=/?ctab=). 28 новых фреймов 11-17 (×Glass/Светлая×десктоп/мобила) + перекапчер главной ×4 (⤢) и контактов ×4 (вкладка). Файл = **69 фреймов**, страница «RU.TV — макеты сайта (v2.15, 26.08)»; 36 призраков «Sidebar - Меню» удалено, ~2030 нод округлено, топ-дроби 0; финальный скрин канваса чист. Раскладка: 11 @19610 → 17 @31376 (шаг 1961) в десктоп-рядах; M11 @4300 → M17 @6880 (шаг 430) в мобильных.

Поставка: **zip → Saved 617832 + HTML → Saved 617833** (617821/617822 устарели). Коммит v2.15 pushed. MXM-002: драфт ответа Стасу (реко fa-bag-shopping) подготовлен — ждёт «OK send» owner'а.
