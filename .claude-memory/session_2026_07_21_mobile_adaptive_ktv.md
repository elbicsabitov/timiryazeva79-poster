# Session 2026-07-21 — Мобильный адаптив Twinr + CRM Турбо (жалоба Татьяны)

## Триггер
Чат @k_t_v_23 (Татьяна Корнеева) 21.07 16:03: «по CRM Turbo, И твинеру (ИИ редакция
и ТГ аудит) моб адаптив ты делал? …я скачала архивы еще раз. но как-то кривовато
выглядят». Задача вытянута телетоном (mama-helper сессия, read-only).

## Диагноз (скриншот-проход, честный 390px)
Ключ: окно Windows не ужимается ниже ~500px (это и был TD-KTV-05) → эмуляция
вьюпорта через CDP `Emulation.setDeviceMetricsOverride` (chrome-devtools MCP
`emulate viewport 390x844x3,mobile,touch`). Дальше объективные пробы
(`scrollWidth > clientWidth`, высоты строк) + скриншоты.

| Поверхность | Что нашлось |
|---|---|
| CRM (архив 06-10) | Мобильного слоя НЕТ вообще: 5 @media, ни бургера, ни off-canvas. Каждый из 29 экранов шире телефона: scrollWidth 635–667 на 390. Рельса сайдбара съедает 120px stage-паддинга. |
| Twinr (архив 07-08) | Шелл-адаптив есть (бургер 06-10), НО: плавающий бургер (fixed 12,12 44px) ЛОЖИТСЯ НА ЗАГОЛОВОК каждой страницы — регресс от удаления топбара 06-17 (все `.page-title` на y=10). |
| ТГ-аудит Обзор | Таблицы «Каналы: ссылки и описания» — строки-гиганты ~250px (описание-колонка за экраном растягивает высоту), ссылки обрезаны; «п/н» ломается на «П/ Н» (перенос по слэшу); «Тематические блоки» текст обрезан. |
| ТГ-аудит Отчётность | «Сводка: план/факт» — колонки % за экраном. |
| ИИ редакция | Фильтры Отбракованных/Опубликованных сжаты в «Кан…»/«Жу…», лейбл «по» оторван. |
| Чат | `html:has(#page-chat.active){overflow:hidden}` при мобильном стеке (height:auto, 1101px) — тач-скролл к композеру под вопросом. |

## Фиксы
**Twinr** (ветка `feat/twinr-tg-audit`, коммит `8a0d51d`, НЕ merged — гейт прежний):
- ≤920: `.stage` padding-top 68px + `--chat-vpad:96px`; ≤560: 64px + 88px. Роутер уже
  делает scrollTo(0) на каждый hashchange — просвет работает везде.
- ТГ-аудит ≤560: `.tgx-ovz-wtable` min-width 620, td wrap, `.tgx-ovz-about` кламп
  4 строки (строки 250→114px), `п/н` nowrap + width 44, ссылки break-all,
  таблицы Отчётности min-width 480 + перенос, Сводка-таблицы то же.
- ИИ редакция ≤560: `.red-tfilters` → grid (селекты/поиск full-width, даты парами).
- Чат ≤920: `html:has(...){overflow:auto}` — стек скроллится.
**CRM** (master `30988ac`, PUSHED):
- Бургер в топбаре (не плавающий — у CRM топбар жив) + scrim + off-canvas сайдбар
  ≤920 (паттерн twinr 06-10 1-в-1, JS IIFE портирован), stage 14px без рельсы,
  kpi-grid 2→1 кол (920/560), crumbs скрыты на 560, ⌘K скрыт, dd-panel max-width,
  таблицы: `.card:has(.t-table)` scroll + `.service-cell` кламп 3 строки
  (строки 155→92px), тач-мишени ≥40, модалки near-fullscreen.

## Верификация
- Twinr: 390 (бургер-дровер, все L1/L2 ТГ-аудита, 9 секций ИИ редакции, чат) +
  десктоп 1440 (stage 24px, chat 852px, b2 min-width 1080 — не тронут). Консоль 0.
- CRM: все 29 страниц h-overflow 0 на 390 (было 635–667 на всех); дровер
  открывается (x:12, w:300, labels visible); десктоп 1440: рельса 72, бургер
  hidden, kpi 4 кол, overflow 0. Консоль 0.

## Доставка
- `twinr-mobile-fix-2026-07-21.zip` (1.27MB) → Saved Messages **msg 604576**
- `crm-mobile-2026-07-21.zip` (0.55MB) → Saved Messages **msg 604577**
- Оба без подписи (канон); Татьяне форвардит Эльбик. README внутри каждого.

## Грабли/уроки
- **Скриншот-капчер CDP таймаутится на CRM** (вечные glass-анимации → capture ждёт
  стабильный кадр). Лечится dpr1 или паузой анимаций (TD-KTV-07).
- `location.hash = '#page-X'` в тестах анкерит страницу на верх ЭЛЕМЕНТА (родной
  якорь браузера) — выглядит как «фикс не сработал»; реальная навигация через
  роутер скроллит к 0. Мерить после явного scrollTo(0,0).
- th с текстом «п/н» ломается переносом ПО СЛЭШУ — nowrap обязателен.
- Двух-worktree дисциплина: twinr-фиксы на своей ветке (gated), CRM на master —
  файлы не пересекаются, stash-dance не понадобился.

## Вечер той же сессии — «База источников» (2-й запрос Татьяны, 16:42)
Спека = md «Общая база источников проекта» (msg 604571, скачан телетоном) + чат:
«отдельный пункт в меню · твинр · сразу с моб адаптивом» («привыкла, что
стартуешь с моб» → standing: запросы Татьяны = mobile-first всегда).

Построено на ветке `feat/twinr-tg-audit` (коммит `deb46ea`, +982 строки,
раздел `page-source-base`, префикс sb-, nav между Хранилищем и Руководством):
- Этап 1 фулл: KPI ×4 → таблица Дата/Тип(TG/WEB)/Название/Источник/Новостей/Теги
  (новые сверху) → все 6 фильтров спеки (период с/по · тип · название · источник ·
  сорт по новостям ↑↓ селектом И кликом по TH · теги-чипы мультивыбор) →
  drill-down в посты источника (по мотивам Сводки-по-каналу) → бейдж
  «Доступ: админ · суперадмин».
- Этапы 2-4 маркерами: «Теги присваивает ИИ» + «⏳ ждёт тегирования» у свежих
  + правило ~20 постов (Этап 2) · модалка «Подписаться на тему» пачкой:
  ТГ-подписки (только публичные) / Источники для рерайтинга (Этап 3) ·
  карточка-резерв рекомендаций (Этап 4).
- Данные — образец; имена/ссылки TG-каналов реальные (из аудит-плана).
- Попутный фикс: 6px h-overflow формы «Новый источник» на старой page-sources
  (inline-grid 4 кол → 1 кол на ≤920).

Верификация: 28/28 страниц twinr h-overflow 0 на 390 · drill/фильтры/сорт/
модалка прогнаны интерактивно · desktop 1440 чист · консоль 0.
Доставка: `twinr-source-base-2026-07-21.zip` → Saved **msg 604649**
(supersede дневного 604576 — тот же файл + новый раздел).
DEBT: KTV-034..036 done + TD-KTV-08 (если Татьяна попросит drill 1:1 с их
прод-«источниками для рерайтинга» — запросить скрины прода).
Гейт ветки прежний: НЕ merged / НЕ pushed.
