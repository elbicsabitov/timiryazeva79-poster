# Session 2026-07-29 — Город ФМ storefront: 4-слойная логика доступа (GOROD-L4)

**Ветка:** `feat/gorod-home-rmg-storefront` · **Файл:** `designs/gorod-fm.html` (~21k строк) · **НЕ запушено**
**Коммиты:** `27e0b5f` (C1 роль-каркас) → `6cd2a25` (C2 гость) → `9b16a0f` (C3 аккаунт) → `765a518` (C4 подписка) → `d5dbe24` (C5 автор) → финальный (C6 верификация+доки).

## Задача от Эльбика
1. `resume gptsong` → сравнение концепции с AI Spotify radio (сделано: Talk to Spotify 14.07.26 и Studio подтверждают вектор; наши отличия — объяснимость/персонажи/поток-первичность — не заняты).
2. «Открой дизайн, что делали до текущего» → поднят :8791, открыт `gorod-fm.html` (v3 storefront).
3. **«Мне эта версия нравится больше — заложи в неё эту логику, как правки в рутв»** + лист 4 слоёв (гость/регистрация/платный/автор-витрина).

## Сделано (метод rutv: прямой Edit + атомарные коммиты + verify после каждого)
- **Каркас:** Tweaks-группа «Роль · слои 1–4» (первая), `gorod-fm.role` → `html[data-role]`, `window.GorodRole` (get/set/atLeast), событие `gorodfm-role-changed`; CSS-маркер `GOROD-L4` в конце первого `<style>` — все послойные правила там.
- **Слой 1 Гость:** композер рейла скрыт (CSS by role) → плашка «слушайте и читайте» + «Войти (демо)»; диджей реагирует репликой на каждую смену роли (`wireRoleLayer`); ad-брейки `#player-ad` (тост слева над мини-плеером, бейдж «Реклама», ~100с/8с, first 25с, ×2 реже при `gorodfm_ad_less`, paid/author не видят); шапка карусели «в демо — 7, в рабочей версии — 1000» (absolute в padding-зоне секции, на мобиле — в потоке).
- **Слой 2 Аккаунт:** blur-гейт `#role-gate` на #/podborki #/taste #/profile #/recap #/onboarding (тексты по роуту, Esc→home, фокус-менеджмент); чип «Под настроение» → 3 настроения → рекомендация с «почему» → «Включай» = `GorodRail.center(id)` + `gorod:tune` (станция реально переключается); развилка free↔paid карточкой в пузыре диджея (`djCard` + delegated `data-upsell`), автопоказ после входа; мост `GorodRailBridge.openUpsell` ← кнопка «Убрать рекламу».
- **Слой 3 Подписка:** секция `#mystream` на главной (lock → build → card); конструктор в «Моём диджее»: перехват свободного текста (`bStep`), чипы-примеры → уточнение «со словами/инструментал/50-50» → `builderMatches` по пулу из 8 треков, живущих в приложении → карточка: per-track «почему», «Не моё» = замена из SWAPS + строка «Учусь: …» (`gorodfm_mystream`), ▶ ставит трек в мини-плеер (диджей комментирует). Фраза «собери мой стрим» тоже стартует. Ниже paid — апселл.
- **Слой 4 Автор:** «Отправить в витрину» → `published` + роль author; полка «Станции слушателей» (`GorodVitrina.renderShelf`, geometry `wide`, после «Друзья слушают», ретрай до `data-vitrina-rendered`), своя первой с live-бейджем «в эфире у всех»; панель автора «214 слушали · 1 840 ₽ — цифры демо — сценарные» + 4 совета (треки/энергия/время рекламы/аудитория) → ответы в чате с «совет сценарный».
- **Cold-start:** в §G4 не-dev гость → `#/home` (лист: «ничего не заполняет»), онбординг вкуса — после входа.

## Верификация (chrome-devtools)
1440 web + 390 mobile (emulate, без h-скролла), cinema + light, все 4 роли end-to-end: гейт-тексты по роутам, mood-переключение станции (active/комната/плеер сошлись), конструктор → карточка → «Не моё» → персист, publish → полка+панель, советы; прод-режим `?dev=0`: cold-start гостя на #/home, Tweaks скрыт, реклама пришла по реальному таймеру 25с, мост «Убрать рекламу» → карточка входа. Консоль: 0 ошибок кода (только favicon.ico 404 — преэкзистинг сервера).

## Грабли/уроки
1. **Смена только hash НЕ перезагружает документ** — после Edit обязательно `navigate reload ignoreCache`, иначе проверяешь старую страницу (поймано: «player-ad missing»).
2. Главная уходит под fixed-топбар (`margin-top:-64`), карусель начинается с `padding-top:96` — свободной зоны всего 32px; absolute-вставки туда мерить, на мобиле переводить в поток.
3. Содержимое `#player-mini` частично перерисовывается модулями — вставки туда позиционировать absolute от бара, а не в поток meta.
4. Полка витрины через `renderShelf` без container + `insertBefore` — рендерится до/после boot безопасно только с ретраем по `data-vitrina-rendered`.

## Открыто
GHS-031 (прод-механика слоёв) · GHS-032 (каталог+LLM для конструктора) · **GHS-033 standalone НЕ пересобран — гейт перед отправкой** · GHS-034 🔒 бренд (ГОРОД.FM vs GPTSONG, два живых файла) · пуш ветки — по «сохрани»/sync.
