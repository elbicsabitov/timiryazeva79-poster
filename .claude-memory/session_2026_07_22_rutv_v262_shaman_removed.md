# Session 2026-07-22 (поздний вечер) — RU.TV v2.6.2: SHAMAN исключён полностью

**Ветка:** `feat/rutv-showcase-v2` · **Коммиты:** `8349d92` (v2.6.2) + sync · **Поставка (актуальная): Saved 605435 (light) + 605436 (dark/Glass)** `*-v4.png` — документы без подписи, Эльбик форвардит. 605346/347 (v2.6.1 с Шаманом) устарели.

## Вход

`resume rutv и из последней версии что мы сделали замени шамана везде на что-то другое чтоб его упоминания вообще не было` + mid-turn «и картинку ты не заменил от шамана» + «как все закончишь дай новые файлы мне в saved как до этого но с обновлением».

## Что сделано

1. **9 текстовых вхождений** «SHAMAN — Я русский» → **«Сергей Лазарев — Шёпотом»**: hero-эфир (`#liveTitle`), PiP, `mainLive`, канал RU.TV `now`, Новые клипы, Супер 20 №1, Артисты, новость-премьера («представил» — род сохранён), страница Видео. Выбор замены: мета «Поп · Эстрада» без правок, ядровой артист RU.TV (многократный лауреат Премии), нигде в демо-данных не дублируется. Длительность честная 3:01 (реальный хронометраж трека).
2. **Картинка (подловил owner)**: `image 1.png` (hero/PiP/`--backdrop-image`/mainLive — 4 ссылки) оказался кадром из клипа «Я русский». Заменён на `assets/rutv/hero-lazarev-shepotom.jpg` — кадр @85s из **официального клипа «Шепотом»** (`_EUkd_bjflE`, канал «Сергей Лазарев», 1080p): `pip install yt-dlp` + ffmpeg (scoop), 12 кадров-кандидатов, отбор по **Laplacian-резкости числом** (85s = 103.6 vs 1.0 у красивого, но блюрного 95s). Кадр = фирменная сцена клипа (Лазарев в чёрном + девушка в вуали на белом) — лейбл↔визуал совпадают, как раньше у Шамана.
3. **Поштучный Read-аудит ВСЕХ ассетов** export'а картинками: Шамана больше НИГДЕ нет. `Rectangle 4322-16.png` под слотами артиста = баннер «Деньги в большом городе» (не персона); плейсхолдеры чарта повторяются группами по 3 — норма демо.
4. Standalone пересобран (`tools/build_showcase_standalone.py`) — 0 вхождений.
5. Макеты v4 по прежней рельсе (1920×1080, applyStyle light/glass, PiP погашен scrollTo(0,0)+600ms, fullpage 2882×8027) → Saved через `.scratch/rutv4/send_fullpage_shots_to_saved.py`.

## Верификация

grep 'шаман|shaman' -i = 0 (aggregator + standalone) · «Я русский» = 0 · DOM-проверка `shamanInDom:false` · консоль 0 · обе fullpage отсмотрены глазами (все секции на месте, hero-заголовок читается на светлом кадре — тёмный градиент снизу работает; play-кнопка по hover — так и было).

## Ловушки / уроки

- **«Упоминания» = текст И картинки.** Первый проход поймал только текст; кадр героя выглядел «нейтральным тёмным», а был кадром артиста. Правило: при вычистке персоны аудировать каждый растр глазами (Read рендерит картинки).
- **Chrome-вкладка `file://` из worktree держит файлы** → `git worktree remove` падает Permission denied. Сначала `close_page`, потом remove (git успевает дерегистрировать — тогда добить `rm -rf` + `git worktree prune`).
- Хвост лога build-тула печатает размер ПОСЛЕДНЕГО варианта (cinematic 4.5MB), агрегатор — строкой выше (9.3MB): не путать.
- yt-dlp на боксе не было — `python3 -m pip install yt-dlp` (scoop shim), ffmpeg уже стоял.

## Гейты / хвосты

- **RUTV-136 (новый, 🔒 gate)**: SHAMAN остался в superseded `showcase-cinematic*.html` (v1) и `smartwatch-rmg.html` — никуда не шлются; чистить только по слову owner'а.
- MXM-002 без изменений (выбор иконки «Магазин» за Эльбиком).
- Zip v2.6.2 не собран (просили картинки) — standalone в репо уже чистый, собрать мгновенно когда попросят.
