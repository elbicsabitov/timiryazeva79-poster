# Session 2026-07-22 — GPTSONG: пивот Город ФМ → chat-first (S1 трека)

**Ветка:** `feat/gorod-home-rmg-storefront` · **HEAD на sync:** `7cf2884` (запушено) · **Файл:** `designs/gptsong.html` (~3.2k строк, single-file SPA)
**Коммиты сессии:** `dee0314` (концепт v1) → `de668b5` (неон-рестайл) → `1d1eab8` (docs) → `f7e7f05` (humanization UX-текстов) → `ef0ce95` (docs) → `7cf2884` (закрытие гэпов 4-слойной логики).

## Задача от Эльбика (цепочка директив за сессию)
1. «resume gorod fm, сохрани текущую версию, гоу новую с правками 15.07» + позже «проект теперь называется GPTSONG».
2. «аудит передовых дизайн-скиллов, гоу топ-сервис» + «все инлайн» + «только проверенные сообществом источники».
3. «не нравится что весь коричневый, глянь как было ранее» + фирменное лого (`designs/assets/gptsong/logo-ref.jpg`).
4. «Муз ред №1 — это примеры были; глянь UX-тексты по нашим топ-стандартам (как презы для Air Astana)».
5. «аудит логика_развития_gptsong (4 слоя) — насколько учтено» → «гоу закрывать».

## Сделано
- **v3 «Город ФМ» сохранена**: `gorod-fm.html` не тронут, тег `gorod-fm-v3-storefront` (=`21c45d2`), ветка+тег на origin.
- **GPTSONG v1** (спека: `docs/superpowers/specs/2026-07-22-gptsong-chat-first-design.md` — маппинг 16 заметок owner'а → решения): чат+избранное+виджет-дека (винил, лампа ON AIR с фликером) на главной; квадратики редакции (главред Сигнал + 5 жанровых, классика Струна в Literata «как литература»); «почему?»-слой → поповер → «Спросить в чате» → ответ главреда (ядро: «правдоподобно объясни — и человек послушает», хедж «могу ошибаться», кнопка «Не моё»); окно «Эфир» (12 демо-потоков, «Новое в эфире» с пересечениями, люди-метчинг 97/91/84/78%, витрина авторов + советы «как заработать больше» [треки/энергия/время рекламы/аудитория], TG-дайджест); вход Telegram/почта БЕЗ Google/Apple; 4 роли + Офис/Банда + Палитра в Tweaks.
- **Дизайн-аудит инлайн** (DESIGN_PROTOCOL + anthropic prompt + frontend-design скилл; без субагентов/внешних фетчей): зерно, staggered boot, localStorage-персист `gptsong.v1`, text-wrap pretty/balance, hit-targets ≥44 моб., приглушённые live-точки.
- **Неон-рестайл по лого** (owner забраковал янтарь): база #08080D/#14141C, бренд-градиент магента→фиолет→синий→циан, вордмарк = SVG-знак (пузырь+нота+эквалайзер) + «gpt» белым + «song» градиентом, Comfortaa (дисплей), лампа ON AIR = неон-магента; ВСЯ палитра на токенах (`--acc*`, `--grad-*`, `--lamp-*`), янтарь жив как `html[data-theme="amber"]` (Tweak «Палитра»), brand-hue в JS = `MAIN_HUES[S.theme]`.
- **Humanization-pass всех UX-текстов** (6 принципов + «сказал бы живой человек?»): квадраты «КЛАССИКА · Струна · слушает как читает» вместо «МУЗ РЕД 1»; подписи «Классика — Струна»; «слой 2» из шапки убран; манифест-рамка вырезана; «в проде»→«в рабочей версии», «пересобрала сборку»→«мы кое-что поменяли», «НЕ-ИИ»→«без ИИ» и т.д.
- **Аудит 4-слойной «логика_развития_gptsong»**: 13/18 полных, 5 частичных → закрыто 4 гэпа: **конструктор «Моей станции» диалогом** (описал → уточнение «слова/инструментал» → профильный редактор вступает → собралось; `myBuilt` персист; вход: карточка рейла / роль-нотис подписки; builder перехватывает composer) · **подборки** (секция в рейле, просмотр, создание чипами, персист) · **«что это?»** на станциях (12 текстов STREAM_ABOUT голосами кураторов) · **режимы Утро/Фокус/Вечер** (modes-теги у треков, фильтр + нотис). Итог 17/18; GS-013 (факты о группах) — ждёт источник метаданных, не выдумываем.

## Верификация
chrome-devtools: desktop 1440/1600 + mobile 390 (emulate), все 4 роли, обе метафоры, обе палитры, конструктор end-to-end, подборки (просмотр+создание), «что это?», режимы, модалки, мосты «почему→чат» — **консоль 0 ошибок** после каждого коммита. Сервер: `cd designs && ~/scoop/shims/python3 -m http.server 8791` (plain `python` в Git Bash нет — exit 127).

## Deviations от SYNC_PROMPT
- Branch НЕ master — осознанно (мульти-трековый репо, канон NEXT.md; push'и по «сохрани текущую версию» + sync).
- Standalone НЕ пересобран — прототип заказчику не отправлялся; для gptsong standalone = GS-001 (сначала заинлайнить Onest+Comfortaa+Literata с Google Fonts + офлайн-гейт по образцу gorod).

## Грабли/уроки сессии
1. Тёплую «студийную» эстетику owner не принял — **канон GPTSONG = неон фирменного лого; коричневое не предлагать** (янтарь остался тумблером для сравнения).
2. «Муз ред №1» из заметок owner'а был примером концепта, не финальной подписью — UX-тексты сразу прогонять через humanization-стандарт (feedback_humanize_partner_texts + humane-ux).
3. Параллельная rutv-сессия разобрала мой стэш rutv-wip (применён, `c009c0e`); правило вперёд: rutv-работа при активной gorod-сессии — только через worktree, ветку основного дерева не трогать.
4. chrome-devtools resize_page может молча не применяться — вьюпорт проверять `window.innerWidth`, надёжнее `emulate viewport`.

## Открыто (DEBT, все с указателями)
GS-001 standalone (гейт перед отправкой) · GS-002 офис/банда — owner · GS-005 каталог/потоки — owner · GS-007 механика метчинга — owner · GS-003/004/006 прод (LLM, TG-login, TG-бот) · GS-013 факты — источник метаданных.
